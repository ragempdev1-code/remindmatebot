# RemindMate Bot

tg бот, считает дни до дня рождения, хранит заметки, шлет напоминания.

## Запуск

```bash
pip install -r requirements.txt
set BOT_TOKEN=твой_токен
python bot.py
```

Токен брать у [@BotFather](https://t.me/BotFather).
